{
  "private": true,
  "scripts": {
    "compile": "browserify ../backbone.marionette/src/backbone.marionette.js --no-bundle-external -t [ babelify --presets [ ./node_modules/babel-preset-es2015 ] ] | ./node_modules/.bin/uglifyjs --compress - > ../backbone.marionette/lib/core/backbone.marionette.js"
  },
  "dependencies": {
    "babel-core": "^6.1.0",
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "uglifyjs": "^2.4.10"
  }
}
